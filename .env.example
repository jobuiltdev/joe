# Copy to .env for local development. Never commit the real .env.

# Contact form delivery. Get a key at https://resend.com
RESEND_API_KEY=

# Set to 1 locally so template caching is off and edits show up without a
# restart. Leave unset in production.
DJANGO_DEBUG=
